<mvc:View
   xmlns="sap.m"
   xmlns:mvc="sap.ui.core.mvc"
   controllerName="sap.ui.demo.walkthrough.controller.InvoiceList">
   <List
      id="invoiceList"
      class="sapUiResponsiveMargin"
      width="auto"
      items="{
         path : 'invoice>/Invoices',
         sorter : {
            path : 'ShipperName' ,
            group : true
         }
      }" >
      <headerToolbar>
         <Toolbar>
            <Title text="{i18n>invoiceListTitle} Lokal"/>
            <ToolbarSpacer/>
            <Label text="{i18n>invoiceSortByText}" />
            <Button text="{i18n>invoiceByName}" press=".onOrder('byName')"/>
            <Button text="{i18n>invoiceByQuantity}" press=".onOrder('byQuantity')"/>
            <Button text="{i18n>invoiceByShipperName}" />

            <SearchField width="50%" search=".onFilterInvoices"/>
         </Toolbar>
      </headerToolbar>
      <items>
         <ObjectListItem
            title="{invoice>Quantity} x {invoice>ProductName}"
            number="{
               parts: [{path: 'invoice>ExtendedPrice'}, {path: 'view>/currency'}], 
               type: 'sap.ui.model.type.Currency',
               formatOptions: {
                  showMeasure: false
               }
            }"
		      numberUnit="{view>/currency}"
            numberState="{= ${invoice>ExtendedPrice} > 50 ? 'Error' : 'Success' }">
            <firstStatus>
					<ObjectStatus 
                  text="{
                     path: 'invoice>Status',
                     formatter: '.formatter.statusText'
                  }" 
                  icon= "{= ${invoice>Status} === 'A' ? 'sap-icon://present' : ${invoice>Status} === 'B' ? 'sap-icon://in-progress' : 'sap-icon://accept'  }"
               />
				</firstStatus>
         </ObjectListItem>
      </items>
   </List>
</mvc:View>